%% Calcular parametros de peso y elementos affine
%   w sera una matriz de 2*n (items de peso de cada elemento V)
%   a sera una matriz de 2*3 (items afin)
%%
function wa = calcParams(W,A)
    wa = [W,A];
end
